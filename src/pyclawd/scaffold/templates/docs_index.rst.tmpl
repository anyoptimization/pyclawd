{{name}} documentation
=======================

This documentation is built by the ``pyclawd docs`` pipeline: markdown sources are
compiled to notebooks, executed (and cached), and rendered to HTML. See
:doc:`example` for a page with a live code cell.

Build it from the repo root::

    pyclawd docs build        # compile -> run -> render
    pyclawd docs serve        # view the result

.. toctree::
   :maxdepth: 1

   example
